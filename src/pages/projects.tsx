import type { NextPage } from "next";
import { Header, ProjectPage, Footer } from "../components";

const Projects: NextPage = () => {
  return (
    <div>
      <Header />
      <ProjectPage />
      <Footer />
    </div>
  );
};

export default Projects;
